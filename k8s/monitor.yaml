apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: node-api-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: node-api
  endpoints:
  - port: web
    path: /metrics
    interval: 15s
    honorLabels: true
    scheme: http